import '../App.css';
function Header(){
   return (
    <nav className='nav'>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Blog</a>
        <a href="#">Contact us</a>
    </nav>
   );
}

export default Header;